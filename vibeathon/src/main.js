// Main entry point
window.addEventListener('load', () => {
    console.log("Dev Chaos Loaded");
    const game = new Game();
    // game.start(); // Start is called by button now, but we need the instance.
});
